import React from 'react'

const Button = ({styles}) => {
  return (
    <button className={`py-4 px-6 bg-blue-gradient font-medium text-primary outline-none text-[18px] rounded-[10px]`}>
        Get Started
    </button>
  )
}

export default Button;